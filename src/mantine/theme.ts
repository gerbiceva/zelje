import { createTheme } from "@mantine/core";

export const mantineTheme = createTheme({
  primaryColor: "cyan",
  primaryShade: 8,
  defaultRadius: "xs",
  // defaultGradient: {
  //   from: "#0e00ff",
  //   to: "#c11a1a",
  //   deg: 20,
  // },
});
